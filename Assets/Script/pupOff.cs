using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pupOff : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void Off()
    {
        this.gameObject.SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
